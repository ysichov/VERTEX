bin/
target/
*.class

# Copied in by vscode:prepublish so the vsix carries the pages.
vscode/resources/
vscode/node_modules/

# Build output of `vsce package`. Every export is a distinct version, so these
# accumulate; the one that matters is attached to a Release, not committed.
*.vsix
