Create an updated polished software architecture infographic based on the attached old VERTEX diagram. Input image is the old architecture and visual style reference. Redesign its layout as needed to add the current AI integration clearly. Output a high-resolution landscape raster image, approximately 1800x1400, with exceptionally legible English typography, white background, pale blue rounded panels, violet VERTEX centerpiece, clean icons and precise orthogonal arrows. No cursor, screenshot artifacts or watermark.

Title: "VERTEX Architecture"
Subtitle: "ABAP tools for Eclipse, VS Code and AI assistants"

Layout:
Top left panel "Editors": "Eclipse / ADT" and "VS Code". Connect each editor downward to its matching host in the central VERTEX panel.
Top right panel "AI assistants": three equal, prominently named cards "Claude Code", "Codex", "GitHub Copilot", with recognizable tasteful icons. Claude Code and Codex use arrows labeled "Manual MCP configuration"; Copilot uses arrow labeled "VS Code MCP provider". These three routes converge exclusively on the "MCP server" box INSIDE the VS Code extension portion of VERTEX, not Eclipse and not directly to SAP.

Central large violet framed panel "VERTEX Tools" with subtitle "Two hosts — shared pages":
Left host "Eclipse plugin" small subtitle "ADT session".
Right host "VS Code extension" small subtitle "Active SAP system + stored credentials".
Shared lower row "Shared pages": "table.html" / "versions.html" / "metrics.html".
Within the VS Code host (or an attached clearly bounded subpanel): "Local MCP server", "127.0.0.1:37777/mcp", "Streamable HTTP + bearer token". Small note "Configurable port · Token in SecretStorage".
Two tool chips within this MCP subpanel: "sap_transport_changes" and "sap_transport_diff". Small note "Read-only · AVE prepared reviews". Show this MCP branch going through the VS Code host HTTP connection to the review endpoint in the lower backend tier. It must be obvious that VS Code must be running and supplies the SAP connection.

Lower pale-blue tier: "SAP ABAP backend" / "ADT hub · one BAdI · /sap/bc/adt/zsde/*" / small "Resources in SDE repository".
The shared pages connect downward to this hub labeled "JSON over HTTP(S) / ADT".
Five endpoint chips horizontally: "table", "join", "versions", "review", "metrics". These are parallel endpoints, NOT a chain: no arrows between endpoint chips.
A distinct violet MCP route connects specifically to "review".

Bottom pale-green tier: "Existing SAP GUI tools"
Three cards:
"SDE" subtitle "Tables, joins, pivot"
"AVE" subtitle "Version history, diffs, prepared reviews"
"ACE" subtitle "Metrics, flow diagrams"
Connect table and join to SDE; versions and review to AVE; metrics to ACE.
Small footer "AI assistants use their own models and subscriptions. VERTEX provides SAP context."

Accuracy matters: AI clients only access the MCP server hosted by the VS Code extension; the Eclipse plugin does not host MCP. Do not imply that MCP writes reviews or prepares them. Avoid excessive text, crossing arrows, tiny labels, missing edges or duplicated nodes. Keep the VERTEX spiral brand motif from the reference. Prioritize clean readable architecture over decorative effects.
