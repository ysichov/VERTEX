Update the attached VERTEX architecture infographic to the CURRENT implementation. Use the old image ONLY as a visual style reference: preserve its white background, crisp navy typography, pale-blue rounded boxes, violet VERTEX banner with spiral motif, green SAP tools footer. Redesign the structure for legibility. Large high-resolution landscape infographic, English text, spacious layout, clean orthogonal labeled arrows, no watermark. Title "VERTEX Architecture", subtitle "Shared UI • Built-in assistants • External MCP clients". Avoid huge decorative logos and microscopic text.

Main diagram: a large left two-thirds area "Built-in Assistant" and separate right-third area "External MCP clients".
LEFT AREA: At top, two equal host cards:
"Eclipse / ADT" with lines "Java plugin + Node bridge" and "SAP access: ADT session".
"VS Code" with lines "Node extension" and "SAP access: configured system".
Both cards sit inside a violet VERTEX Tools boundary. A shared horizontal bar beneath them: "Shared pages: Selector • Versions • Metrics".
Below that, within same area, a workflow with explicit bidirectional arrows:
"Assistant panel (Selector / Versions)" → "Shared assistant runtime" → "Codex CLI / Claude Code CLI".
Label runtime-to-CLI arrow "Launch request"; show CLI-to-runtime returning arrow "JSON plan / review".
Under runtime show attached small subbox "Private MCP tools" with text "Layouts • Versions • Review diffs", note "Loopback HTTP + bearer token".
Connect CLI bidirectionally to that private MCP subbox labeled "Read context". Connect subbox to the two host SAP-access cards or clearly label "SAP reads through the selected host".
Another arrow from runtime back to Assistant panel labeled "Validate plan → Apply view". Small note "Selector: metadata only; query rows stay in the UI". Small note in Eclipse card "No SAP password passed to CLI".
Do NOT route Copilot through these built-in CLI processes.

RIGHT AREA: "External MCP clients" heading with "GitHub Copilot • Codex • Claude Code".
Show TWO clearly separate route cards beneath external clients with arrows from clients:
1. "VS Code MCP provider / HTTP" with "VS Code running" and "SAP connection from extension".
2. "Standalone Node MCP / stdio" with "Works with Copilot in Eclipse" and "Own SAP connection settings".
Both route cards connect to a shared small label "Read-only transport tools" and two exact names, each its own line: "sap_transport_changes" and "sap_transport_diff". Arrow downward from this external branch must go ONLY to review resource in SAP tier, label "SAP HTTP(S)". Do not say that Eclipse Java plugin hosts an external MCP server or that standalone supports Selector. This external branch is independent of built-in Assistant.

FULL WIDTH lower tier "SAP ABAP backend" subtitle "ADT resources: /sap/bc/adt/vertex/*". Five parallel chips "table", "join", "versions", "review", "metrics". No arrows between chips. Left host branch connects to backend labeled "JSON over HTTP(S) / ADT"; external MCP branch connects to "review" only.
Bottom tier "Existing SAP GUI tools": three cards "SDE — Tables, joins, pivot", "AVE — Versions, diffs, prepared reviews", "ACE — Metrics, flow diagrams". Connect table and join down to SDE; versions and review down to AVE; metrics down to ACE.
Footer "Assistants use their own models and subscriptions. VERTEX provides SAP context."
Highest priority: truthful connection paths, legible labels and clear separation of built-in Assistant vs external MCP. Shorten nonessential annotations rather than crowding. Do not copy old obsolete claim that AI support is VS Code only.
