{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run VERTEX extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode",
        "--disable-extensions"
      ]
    }
  ]
}
