{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SelecTor extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode",
        "--disable-extensions"
      ]
    }
  ]
}
